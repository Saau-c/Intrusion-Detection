# Intrusion-Detection
The project is built in order to detect the unusual traffic in our network <br>
In order to run this project, on your terminal, go in your project directory. <br>
run the command "py app.py", the app will run locally. <br>
*Note: The app is compatible with python ver. 3.8

